jaffle_shop:
  outputs:

    dev:
      type: postgres
      threads: 1
      host: localhost
      port: 5432
      user: user
      pass: password
      dbname: db
      schema: jaffle_shop

  target: dev
